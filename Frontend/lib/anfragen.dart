import 'package:flutter/material.dart';

Widget anfragen() {
  return const Center(
    child: Column(
      mainAxisAlignment: MainAxisAlignment.center,
      children: [
        Text("Anfragen"),
        Icon(Icons.search),
      ],
    ),
  );
}
